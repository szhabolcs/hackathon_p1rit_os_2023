class GroceryModel {
  late int id;
  late int userId;
  late String storeName;
  late double sum;
  late DateTime date;
  late String query;

  GroceryModel(this.id, this.userId, this.storeName, this.sum, this.date, this.query);
}